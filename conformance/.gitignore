# Per-run output (TestNG XML, TE container logs, summary text).
output/

# Generated by run.sh at compose-time; carries the ETS git build context.
compose.override.yml

# Reserved: vendored ETS source if we ever switch from git-URL build to clone.
.vendor/
